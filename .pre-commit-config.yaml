repos:
  - repo: local
    hooks:
      - id: bootstrap-aws
        name: Bootstrap AWS before push
        entry: aws/scripts/check-and-bootstrap.sh
        language: script 
        pass_filenames: false
        verbose: true
        stages: [pre-push]

